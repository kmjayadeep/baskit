{
  "name": "@baskit/autonomous-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bounded local Pi runner for reviewed Baskit development pull requests",
  "bin": {
    "autonomous-agent": "./dist/cli.js"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run test",
    "test:coverage": "vitest run test --coverage",
    "lint": "eslint src test",
    "check": "npm run typecheck && npm test && npm run lint"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.84.4",
    "typebox": "1.3.7",
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@types/node": "22.19.19",
    "@vitest/coverage-v8": "4.1.9",
    "eslint": "9.39.1",
    "typescript": "5.9.3",
    "typescript-eslint": "8.52.0",
    "vitest": "4.1.9"
  }
}
